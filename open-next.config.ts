// OpenNext config — tells the adapter how to build this site for Cloudflare.
// Defaults are right for a stock Next.js app with no cache, queue or
// database. Options get added here only if that stops being true.

import { defineCloudflareConfig } from "@opennextjs/cloudflare";

export default defineCloudflareConfig({});
